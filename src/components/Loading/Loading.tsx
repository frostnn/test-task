import { LoadingIcon, LoadingIconWrapper } from './styles';

const Loading: React.FC = () => {
  return (
    <LoadingIconWrapper>
      <LoadingIcon />
    </LoadingIconWrapper>
  );
};

export default Loading;
